Команды для регистрации ваших
write 1 time for the computer
git config --global user.name "tilenur"
git config --global user.email "tilenur@gmail.com"
========================================================================
write 1 time for the project
git init
git remote add origin https://github.com/tilenur/oop-63-2.git
========================================================================
Основные команды для работы с GIT

git status

need to create file .gitignore

git add .

git add \file name or directory
git add lessons/lesson2Teacher.py (tab - auto add)
git add lessons/ (all folder)

git rm -r .name or directory

git commit -m "_______"

git branch                  -- to check which branch


git push origin master      -- Windows
git push origin main        -- Mac

===========================================================

git branch - see branch
git branch (name of the branch)     -- for creating branch

git merge (branch name)

git switch (branch name)     - for switching branches
git checkout (branch name)

git switch -c (branch name)  - for creating branch and switching it to it
git checkout -b (branch name)

Branches:
1. prod
- release
2. test
3. stage|dev|test
4. dev

Branches names:
- feat/login
- feat/register
- feat/comment

Commit names:
git commit -m "feat: _____"
git commit -m "update: _____"
git commit -m "fix: _____"
git commit -m "hotfix: _____"

git push origin (name of the branch)


////////////////////////////////////////////////////////////////

git restore __file__/__place__     - restores state to the last commit
git restore .                      - restores all files
git reset .                        - restores to previous state after add . (buffer obmena)

git reset --mixed HEAD~1           - delete last commit
git restore .                      - restores all files at that moment

////////////////////////////////////////////////////////////////
cloning github to my local folder

1) cd ../                               - leave the project/folder
2) ls                                  - check where I am
3) mkdir ___(folder name)___           - making project/folder
4) cd ___(folder name)___              - enter file
5) git clone ___(github link)___       - cloning github link

6) ls                                  - check where I am
7) cd ___(folder)___                   - enter the folder
8) git switch -c ___(branch name)___       - creating and moving to the new branch
9) git push origin ___(branch name)___ - pushing the code into the new branch

